# Менеджер автозапуска GNOME

🌍 **[Türkçe](README.tr.md) | [English](README.md) | Русский | [Български](README.bg.md)**

---

> Я искал единственное приложение, которое могло бы как управлять автозапуском GNOME, **так и** работать в качестве полноценного планировщика задач — запускать скрипты с заданным интервалом, при загрузке или в конкретное время. Я не смог найти ничего подходящего, поэтому разработал это сам.

---

Современный GTK-менеджер автозапуска GNOME с полноценным **Планировщиком задач** на базе `systemd --user`. Написан полностью на Python — никаких дополнительных зависимостей.

Приложение **автоматически определяет язык системы** и отображается на турецком, английском, русском или болгарском.

---

## ✨ Возможности

### Менеджер автозапуска
- **Настоящие иконки** — отображает иконки из `.desktop`-файлов
- **Статус и ресурсы в реальном времени** — CPU (%) и RAM (МБ/ГБ) с отслеживанием PID
- **Множественный выбор** — запуск, остановка или удаление нескольких приложений одновременно
- **Безопасная остановка** — использует PID-файлы; никогда не завершает не тот процесс
- **Терминальный режим** — запуск скриптов в видимом окне GNOME Terminal
- **Задержка запуска** — настройка задержки в секундах для каждого приложения

### Планировщик задач (альтернатива cron)
- **Интервал** — запуск каждые N минут / часов / дней
- **Календарь** — запуск в определённые дни и время (напр. каждый понедельник в 09:00)
- **Загрузка** — при запуске системы (в фоне, без входа при Lingering)
- **Вход** — при открытии сессии
- **Ручной запуск** — кнопка «Запустить сейчас»
- **Поддержка Lingering** — одним кликом `loginctl enable-linger` из Настроек

### Общее
- **Системный трей** — работает в фоне с иконкой в трее
- **Память окна** — запоминает размер и положение
- **Многоязычность** — Турецкий 🇹🇷, Английский 🇬🇧, Русский 🇷🇺, Болгарский 🇧🇬

---

## ⌨️ Горячие клавиши

| Сочетание | Функция |
|-----------|---------|
| `Ctrl + N` | Добавить новое приложение автозапуска |
| `Ctrl + E` | Изменить выбранное приложение |
| `Ctrl + S` | Запустить выбранное приложение |
| `Ctrl + K` | Остановить выбранное приложение |
| `Delete` | Удалить выбранное приложение |
| `Ctrl + F` | Перейти в поле поиска |

---

## 🚀 Установка

```bash
git clone https://github.com/Nikolayco/Gnome-Startup-Applications-Manager.git 2>/dev/null || true
cd Gnome-Startup-Applications-Manager
bash install.sh
```

Команда устанавливает приложение в `~/.local/bin` и автоматически добавляет его в **сетку приложений (App Grid)**.

**Требования:** Python 3.8+, GTK 3, systemd

---

## 🗑️ Удаление

```bash
git clone https://github.com/Nikolayco/Gnome-Startup-Applications-Manager.git 2>/dev/null || true
cd Gnome-Startup-Applications-Manager
bash uninstall.sh
```

> **Примечание:** Ваши собственные записи автозапуска не будут удалены — удаляются только файлы, созданные этим приложением.

---

*Разработчик: Nikolayco *
